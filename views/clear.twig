{% extends 'layout.twig' %}

{% block alert %}

{% if errors|length > 0 %}
    <div class="alert alert-error">
        <ul>
        {% for error in errors %}
            <li>{{ error }}</li>
        {% endfor %}
        </ul>
    </div>
{% endif %}
{% if message %}
    <div class="alert alert-success">
        {{ message }}
    </div>
{% endif %}

{% endblock %}

{% block body %}
<section class="highlight">
    <p>Algolia Extended give you the power to clear an environment. It will delete all indices presents into it.<p>
    <p>Be aware that this can't be undone, and you will lost all your datas. We suggest you do to a BACKUP <!--an <a href="/export">Export</a>--> first!</p>
</section>

<form method="post" action="/clear" id="form-clear" class="highlight form">
    <div class="form-group">
        <label for="target_app_id">Target App Id</label>
        <input type="text" class="input" name="target_app_id" required id="target_app_id" />
    </div>
    <div class="form-group">
        <label for="target_app_key">Target Admin Key</label>
        <input type="password" class="input" name="target_app_key" required id="target_app_key" />
    </div>

    <div class="form-group form-buttons">
        <button type="reset" class="button-left">Reset</button>
        <button type="submit" id="clear-submit" class="button-right">Submit</button>
    </div>
</form>

{% endblock %}
